# groups rows that have same values
# into summary rows

# it collects data from multiple records
# and groups the result by one
# or more column

# generally we use group by with some
# aggregation functions

# count number of students in each day
SELECT city, count(name)
FROM studnet
GROUP BY city;

# can also do multiple
SELECT city, name , count(name)
FROM student
GROUP BY city, name;