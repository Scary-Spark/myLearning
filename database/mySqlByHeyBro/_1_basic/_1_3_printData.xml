# select some data
SELECT first_name, last_name FROM employees;

# select some data
SELECT * FROM employees
WHERE employee_id>2 AND employee_id<=4;

# select some data
SELECT * FROM employees
WHERE employee_id=2 ;

# select some data
SELECT * FROM employees
WHERE hire_date IS NULL;

# update data
UPDATE employees
SET hourly_pay = 10.25,
    hire_date = "2023-01-27"
WHERE employee_id = 6;

# update all column
UPDATE employees
SET hourly_pay = 10.50;

# delete row
DELETE FROM employees
WHERE employee_id=6;